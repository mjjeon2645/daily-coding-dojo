public class Solution {
  public int solution(String dartResult) {

    int answer = 0;
    int firstGameCount = 0;
    String bonus = "SDT";

    int firstNumber = Character.getNumericValue(dartResult.charAt(0));

    for (int i = 0; i < bonus.length(); i += 1) {
      if (dartResult.charAt(1) == 'S') {
        MfirstGameCount
      }

      dartResult.charAt(1) ==



    firstGameCount = Math.pow(firstNumber,

//    if (dartResult.length() == 6) {
//      for (int i = 0; i < dartResult.length(); i += 2) {
//        char defaultCount = dartResult.charAt(i);
//        char area = dartResult.charAt(i + 1);
//
//        int base = Character.getNumericValue(defaultCount);
//        int value = 0;
//
//        switch (area) {
//          case 'S' -> value = (int) Math.pow(base, 1);
//          case 'D' -> value = (int) Math.pow(base, 2);
//          case 'T' -> value = (int) Math.pow(base, 3);
//        }
//
//        System.out.println(value);
//
//        answer += value;
//      }
//    }
    return answer;
  }
}
