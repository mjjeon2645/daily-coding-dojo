export function check(board, n) {
  if (n === 1) {
    return 1;
  }

  return 1;
}

export default function solution(n) {
  const answer = 1;

  const board = Array(n).fill(0);

  const result = check(board, n);

  return answer;
}
