import solution, { check } from './solution';

test('Solution', () => {
  expect(solution(1)).toBe(1);
  expect(solution(2)).toBe(0);
});

test('check', () => {
  expect(solution(1)).toBe(1);
  expect(solution(2)).toBe(0);
});
