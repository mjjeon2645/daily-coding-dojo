// eslint-disable-next-line import/prefer-default-export
export function solution(array, commands) {
//   const answer = [];

  const numberForTest = array.slice(commands[0][0] - 1, commands[0][1])
    .sort().;

  return numberForTest;
}
